import {StyleSheet} from "react-native";

export const FasterModeStyle = StyleSheet.create({
    ModeContainer: {
        alignItems: 'center',
        justifyContent: 'space-between',
        flex: 1
    },
    textStyle: {
        alignItems: 'center',
        paddingTop: 30
    }
});
